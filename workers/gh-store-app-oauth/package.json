{
  "name": "gh-store-app-oauth",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker handling the GitHub OAuth start/callback hop for the Komi Store mobile app.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:staging": "wrangler deploy --env staging",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250501.0",
    "typescript": "^5.4.0",
    "wrangler": "^3.114.0"
  }
}
